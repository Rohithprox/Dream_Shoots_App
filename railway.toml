# Dream Shoots App - Railway Configuration
# For separate services, add them individually in Railway Dashboard

[build]
builder = "dockerfile"

[build.dockerfile]
path = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyMaxRetries = 5
